{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main WarStonks window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default"
  ]
}
